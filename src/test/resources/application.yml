spring:
  datasource:
    url: jdbc:h2://mem:test_db;DB_CLOSE_DELAY=-1;MODE=MSSQLServer
    username: sa
    password: sa
    driverClassName: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false